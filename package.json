{
  "name": "assignment3-night-harbor",
  "version": "1.0.0",
  "private": true,
  "description": "Night Harbor cargo game with fixed-step simulation, interpolated rendering and deterministic replay",
  "scripts": {
    "test": "node tests/verify.cjs"
  }
}
